# Google Merchant Center Account ID (numeric, found in GMC top-right)
GMC_MERCHANT_ID="your-merchant-id-here"

# Authentication — choose ONE of:
#   1) Service Account (recommended for internal tools):
#      export GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
#   2) gcloud ADC (for local dev):
#      gcloud auth application-default login --scopes=https://www.googleapis.com/auth/content
